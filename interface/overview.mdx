---
title: "Armorsmith Designer Interface Overview"
sidebarTitle: "Overview"
description: "Learn the key areas of the Armorsmith Designer interface, including the 3D Viewport, Property Grid, Toolbar, and Menu Bar."
---

Armorsmith Designer organizes its workspace into a set of focused panels that work together — you build and refine your costume design entirely within a single window, moving between tools as your project grows. Getting familiar with where each panel lives will make your workflow faster and more intuitive from the start.

## UI Regions at a Glance

The application window is divided into the following major areas:

<CardGroup cols={2}>
  <Card title="Menu Bar" icon="bars">
    Runs across the very top of the window. Contains the **File**, **Avatar**, **Windows**, and **Help** menus, giving you access to project management, avatar controls, panel toggles, and application settings.
  </Card>
  <Card title="Toolbar" icon="toolbox">
    Sits just below the Menu Bar. Provides quick one-click access to the most common tools, including selection and transform (move, rotate, scale) modes. Switch tools here instead of hunting through menus.
  </Card>
  <Card title="3D Viewport" icon="cube">
    The large central area that takes up most of the screen. This is where your 3D avatar and all attached costume parts are displayed. All navigation and hands-on manipulation happens here.
  </Card>
  <Card title="Property Grid" icon="table-list">
    A panel — usually docked to the right side — that displays the editable properties of whatever object you currently have selected: position, rotation, scale, and material. If you don't see it, see the note below.
  </Card>
</CardGroup>

## Tips for First-Time Users

- **Start with the avatar.** Before attaching any 3D models, use **Avatar → Edit Avatar** to enter your measurements. The entire template-generation system scales to that avatar, so accurate measurements are critical.
- **Look for panel headers.** Each panel has a title bar you can use to drag and re-dock it. If a panel seems to have disappeared, it may have been moved off-screen or collapsed — check the **Windows** menu to restore it.
- **Use the Toolbar for everyday actions.** Reaching for a menu every time you want to switch from selection to move mode slows you down. Pin the Toolbar into your muscle memory early.
- **The Property Grid is your precision tool.** Clicking and dragging objects in the viewport is great for rough positioning, but typing exact values into the Property Grid is how you get precision fit.

<Note>
  **Property Grid not visible?** A known issue in some versions of Armorsmith Designer causes the Property Grid panel to be hidden by default after an update. If you can't see it, go to **Windows → Property Grid** in the Menu Bar to make it visible again. The panel will reappear in its last docked position.
</Note>
